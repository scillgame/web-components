import { GetTranslationPipe } from './get-translation.pipe';

describe('GetTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
