import { CompletedLevelsPipe } from './completed-levels.pipe';

describe('CompletedLevelsPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedLevelsPipe();
    expect(pipe).toBeTruthy();
  });
});
