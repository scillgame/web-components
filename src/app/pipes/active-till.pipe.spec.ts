import { ActiveTillPipe } from './active-till.pipe';

describe('ActiveTillPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveTillPipe();
    expect(pipe).toBeTruthy();
  });
});
