import { CompletedChallengesPipe } from './completed-challenges.pipe';

describe('CompletedChallengesPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedChallengesPipe();
    expect(pipe).toBeTruthy();
  });
});
